# LINE Messaging API credentials (template)
# Copy this file as `line.env` and fill the values locally.
# Do NOT commit real secrets.

# Cloud Run Secrets (已建立於 Secret Manager 並綁定到 n8n):
# - line-channel-id (env: LINE_CHANNEL_ID)
# - line-channel-secret (env: LINE_CHANNEL_SECRET)
# - line-channel-access-token (env: LINE_CHANNEL_ACCESS_TOKEN)
# Service account: <N8N_COMPUTE_SA_EMAIL>
# Role: roles/secretmanager.secretAccessor

LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=

# Optional: Webhook URL used by LINE to notify your server.
# Example: https://<N8N_HOST>/webhook/line-webhook
LINE_WEBHOOK_URL=


在 n8n 工作流中使用

使用表達式語法讀取環境變數：
{{$env.LINE_CHANNEL_ID}}
{{$env.LINE_CHANNEL_SECRET}}
{{$env.LINE_CHANNEL_ACCESS_TOKEN}}
